## 阶段 1: 确认 GA 注入入口
**目标**: 确认全站布局文件与脚本注入位置。
**成功标准**: 明确在 `app/layout.js` 注入最小改动方案。
**测试**: 人工检查 `app/layout.js` 结构。
**状态**: 已完成

## 阶段 2: 实施生产环境 GA 注入
**目标**: 在生产环境加载 gtag.js 并初始化 `G-ZQP0K724BY`。
**成功标准**: 仅生产环境输出 GA 脚本，开发环境不注入。
**测试**: 代码检查 `process.env.NODE_ENV` 条件渲染与脚本内容。
**状态**: 已完成

## 阶段 3: 校验与收尾
**目标**: 运行 lint 并同步计划状态。
**成功标准**: `npm run lint` 无新增 error。
**测试**: 执行 `npm run lint`。
**状态**: 已完成
