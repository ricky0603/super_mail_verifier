## 阶段 1: 定位首页 CTA 登录态渲染逻辑
**目标**: 找到首页 Hero 区域按钮在登录后变成头像和用户名的来源。
**成功标准**: 明确是营销页 CTA 复用了登录按钮组件，并在登录态下走了账户展示分支。
**测试**: 检查首页 `app/page.js` 与 `components/ButtonSignin.js` 的调用和渲染逻辑。
**状态**: 已完成

## 阶段 2: 修复 Hero 按钮文案与跳转
**目标**: 让首页 Hero 的主 CTA 在登录后仍显示 `Verify Now`，点击跳到 `/dashboard`。
**成功标准**: 仅 Hero CTA 改为固定文案模式，不影响其他使用同一组件的账户入口。
**测试**: 检查首页 Hero 的调用参数和 `ButtonSignin` 的登录态分支。
**状态**: 已完成

## 阶段 3: 验证与收尾
**目标**: 运行代码检查并确认这次 CTA 改动没有引入新的 lint 问题。
**成功标准**: `npm run lint` 通过，计划文件状态与实际一致。
**测试**: 运行 `npm run lint`。
**状态**: 已完成
