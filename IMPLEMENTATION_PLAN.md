## 阶段 1: 规划定价区块重构
**目标**: 明确定价区块改造范围：移除计算器、替换标题文案、改为带 CTA 的套餐卡片。
**成功标准**: 形成可实施的页面结构与字段设计，不影响其他区块。
**测试**: 人工检查 `app/page.js` 中 pricing 区块结构。
**状态**: 已完成

## 阶段 2: 实施页面改造
**目标**: 在 `app/page.js` 重写 pricing 区块为卡片式套餐展示，并加入 CTA 按钮。
**成功标准**: 页面不再出现 ROI Calculator；标题改为“按用量选择计划”语义；每个计划都有 CTA。
**测试**: 本地静态检查 JSX 结构、类名与文案。
**状态**: 已完成

## 阶段 3: 验证与收尾
**目标**: 运行 lint 并清理计划文件。
**成功标准**: `npm run lint` 无新增 error；完成后删除 `IMPLEMENTATION_PLAN.md`。
**测试**: 执行 `npm run lint`。
**状态**: 进行中
